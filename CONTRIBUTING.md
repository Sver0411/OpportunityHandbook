# 参与贡献

这是一个公共参考手册，目标是**很多年后还能被人回来查**。所以内容的准确性、来源和时效比数量重要得多。

## 适合贡献什么

- 补充某一章里已规划但还没写的条目（文件里标着 `status: todo`）。
- 更正过期信息：政策、考试、签证、招聘规则变了，把正文改掉并更新 `last_verified`。
- 指出没有来源的判断，或补上更权威的来源。
- 修正结构问题：错别字、断链、重复内容、无效的内部锚点。
- 补充专题资料（`docs/` 下的专题手册、时间线、国家、行业、科研、竞赛、工具模板），但**不要复制正文**：专题只补资料与索引，判断链接到主线。

不适合贡献什么：

- 营销式内容、招生软文、引流链接；
- 没有来源的判断被写成事实；
- 从其它项目复制来的正文；
- 为了凑数量而生成的、没有具体信息的条目。

## 新增或修改一篇内容

1. 先确认这件事**只有一个 canonical 版本**：一个核心问题只保留一份正文，其他位置放链接（包括专题页与时间线页）。
2. 在主线栏目（`book/00`–`book/11`）里找位置：按「读者什么时候会遇到这个问题」放，而不是按学科放。
3. 按 [条目格式](meta/条目格式.md) 写 `### 标题`、```meta 块，正文写成正常文章，结尾统一写 `## 来源与更新`。
4. 元数据取值必须来自 [元数据规范](meta/元数据规范.md) 的枚举；`status: complete` 时必填项见规范（含 `summary`）。
5. 从旧格式改成文章格式时，可以用 `python3 tools/naturalize.py` 的写法参考；旧地址会通过
   `meta/ia-redirects.json` 自动重定向，**不要手写重定向，也不要把文章搬回旧文件**。
6. 正文语言遵循 [写作规范](meta/写作规范.md)：直接、具体、克制，不用「毫无疑问」「强烈推荐」这类表述。
7. 每条来源后面标出证据类型，链接给到原始页面而不是转载页。只用二手来源时必须注明「二手，未经官方核实」。
8. 不要编数字。没有把握的薪资、录取率、通过率，写「不同情况差异很大，需要按目标岗位核实」。
9. 把 `last_verified` 更新为你**实际打开过来源页面**的日期（meta 块与正文的「最后核实」应当一致）。

## 允许这样写

```
暂未找到可靠依据。
不同情况差异很大，需要按具体项目核实。
该说法目前只有二手来源，未经官方确认。
```

把不确定的事情写得很确定，是这个项目最严重的错误类型。

## id 与链接规则

- `id` 全仓库唯一，只用小写字母、数字和连字符，**不要重命名已发布的 id**（会影响外部链接与锚点）。
- 跨文件引用条目：`[目标标题](04-当你开始面对第一次重要分流.md#grad-school-worth-it)`，锚点用条目 id，链接文字必须是目标标题本身。
- 构建工具会校验目标文件与锚点是否存在。

## 提交之前

```bash
python3 tools/build.py --strict --report   # 必须通过：元数据、重复 id、内部链接、构建、外链永久失效
python3 tools/run_tests.py                 # 内容与工程规则测试
python3 tools/smoke_test.py                # 有 Chrome 时：前端冒烟测试（可选）
python3 tools/audit_claims.py             # 高风险绝对化措辞报告（只报告，不阻塞）
```

`--strict` 的含义：任何警告（含元数据解析警告）都会失败；外链里 403 / 429 / 5xx / 超时只警告，
但 404 / 410 / 域名不存在会失败。CI 与 Pages 用的是同一个入口，所以本地过不了的改动不会被部署。

校验规则都在 `tools/meta.py` 里，改规则请同时更新 [元数据规范](meta/元数据规范.md)。

## 还没写的内容怎么标记

未撰写的文档用 `status: planned`：它们**不会**出现在在线导航、搜索结果与筛选里，只汇总在
[docs/ROADMAP.md](docs/ROADMAP.md)。写完后把 `status` 改成 `complete` 并补上 `last_verified`，
构建会检查路线图里有没有遗漏的条目。

`book/` 与 `docs/` 的分工见 [写作规范](meta/写作规范.md)：正文写判断与方法，专题写资料与模板，
专题里不要重复正文已经给出的判断。

## 提交信息

用一句话说清改了什么，例如：

```
补充 03 章“工作后读研”条目并更新 last_verified
修正留学申请时间线中的签证链接
```

## 目录与文件名约定

- `book/` 下的文件名格式为 `NN-章节名.md`，编号与 front matter 的 `order` 必须一致。
- 新增章节需要同时更新：`book/README.md`、根 `README.md` 的目录表、以及 `docs/README.md` 中相关的交叉引用。
- 不要重命名既有文件：外部已经有人链接到它们。

## 许可证

提交即表示同意正文以 CC BY 4.0、代码以 MIT 发布（见 [LICENSE-CONTENT](LICENSE-CONTENT) 与 [LICENSE](LICENSE)）。
