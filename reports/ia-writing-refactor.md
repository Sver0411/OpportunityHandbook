# IA 与写作模型重构报告

本轮不新增正文，只做三件事：**信息架构重排、正文模型改造、少量文章自然化**。

- 仓库：https://github.com/Sver0411/OpportunityHandbook
- 在线：https://sver0411.github.io/OpportunityHandbook/
- 目标：让用户「从上往下读，内容跟着人生问题往前走」，同时「想单独查科研、竞赛、语言、国家时能立刻进去」。

---

## 1. 旧目录与新目录如何映射

主线从「按学科分类」改为「按你会先遇到什么」排列，一级栏目由 17 个学科章节变成 **12 个**：

| 新栏目 | 内容来自（旧文件） |
| --- | --- |
| 00 从这里开始 | `00-从这里开始` ＋ `02-怎么判断一个机会值不值得`（判断框架与关键概念） |
| 01 先决定下一步往哪里走 | `01-十八岁以后有哪些路` ＋ `03-升学` 的专升本 ＋ `12-奖学金资助` 的学生资助与助学贷款 |
| 02 在学校里先把基础打好 | `11-技能语言与证书` ＋ `15-不知道自己想做什么`（低成本试错） |
| 03 开始积累真正能留下来的经历 | `10-项目作品与开源` ＋ `09-竞赛与挑战` ＋ `08-科研与学术`（科研入门/找导师）＋ `13` 的社群 ＋ `05` 的实习准备 |
| 04 当你开始面对第一次重要分流 | `03-升学` ＋ `04-留学与海外教育` ＋ `08-科研与学术`（做研究/产出/伦理）＋ `05` 的实习升级 ＋ `12` 的资助 |
| 05 从学校走向第一份工作 | `06-找工作` ＋ `05` 的第一份工作 |
| 06 进入职场以后继续积累 | `07-工作后的发展`（头几年）＋ `13` 的 Mentorship 与行业影响力 |
| 07 当职业开始出现分岔 | `07-工作后的发展`（晋升/跳槽/转岗转行/再教育） |
| 08 其他同样成立的人生路径 | `14-创业自由职业与副业` |
| 09 专题手册 | 新建 10 个专题索引页（科研/竞赛/项目与作品/开源/技能/语言/证书/资助/社群/创业）＋ `docs/research`、`docs/competitions` 等原专题 |
| 10 时间线与工具 | `docs/timelines` ＋ `docs/tools` |
| 11 避坑 | `16-避坑` |

具体到条目的归属见 `tools/migrate_ia.py` 里的 `PLAN`（声明式结构，条目按 id 归位）。

## 2. 哪些文件移动 / 重命名

- **新建 10 个章节文件**：`book/00-从这里开始.md`、`01-先决定下一步往哪里走.md`、`02-在学校里先把基础打好.md`、`03-开始积累真正能留下来的经历.md`、`04-当你开始面对第一次重要分流.md`、`05-从学校走向第一份工作.md`、`06-进入职场以后继续积累.md`、`07-当职业开始出现分岔.md`、`08-其他同样成立的人生路径.md`、`11-避坑.md`。
- **删除 16 个旧章节文件**（内容已全部搬入新文件）：`01-十八岁以后有哪些路`、`02-怎么判断一个机会值不值得`、`03-升学`、`04-留学与海外教育`、`05-实习与第一份工作`、`06-找工作`、`07-工作后的发展`、`08-科研与学术`、`09-竞赛与挑战`、`10-项目作品与开源`、`11-技能语言与证书`、`12-奖学金资助与免费机会`、`13-社群导师与行业影响力`、`14-创业自由职业与副业`、`15-不知道自己想做什么`、`16-避坑`。
- **新增 10 个专题索引页**：`docs/manuals/*.md`（只列链接，不复制正文）。
- **39 个专题文档**的 front matter 增加 `section`（09/10）与 `subsection`，正文未动。
- 搬移是**逐条按 id**完成的：脚本在写文件前校验「233 条全部归位、没有遗漏」，写完后再比对前后 id 集合（这次一次通过，没有丢内容）。

## 3. 哪些只是改变 section，不改变正文

- `docs/` 下 39 个 planned 专题 ＋ 5 个工具模板 ＋ 10 个新索引页：只改分区与子分类，正文原样。
- `meta/` 下 4 份规范：改为 `nav: false`（它们是给贡献者看的，不再出现在读者导航里），正文只做了与新模型相关的更新。
- 144 篇未迁移的条目：只换了所在文件与分组，**正文一字未改**（仍保留旧栏目结构，见第 9 节）。

## 4. 旧 deep link 如何兼容

- 迁移时生成 `meta/ia-redirects.json`：**文档级 16 条 + 条目级 218 条**（`#/doc/<旧位置>/<条目 id>` → `#/doc/<新位置>/<条目 id>`）。
- 前端在路由解析时先查这张表：命中就用新地址渲染，并用 `history.replaceState` 把地址栏换成规范地址（分享出去的链接会自动收敛到新地址）。
- 测试保证两侧都存在：`Redirects::test_old_docs_resolve`（旧位置必须已不存在、新位置必须存在）与 `test_old_entries_resolve`（每个重定向目标都必须是真实条目）。
- 冒烟测试新增「旧深链重定向」用例：打开 `#/doc/book/03-升学/grad-school-worth-it`，断言文章能渲染、地址被改写成 `#/doc/book/04-…`。

## 5. 固定正文模板如何取消

| 位置 | 改动 |
| --- | --- |
| `meta.py` 校验 | 不再要求正文存在「一句话」栏；不再把「缺少固定栏目」当问题 |
| 摘要 | 不再从「一句话」抽取，见第 6 节 |
| 时效一致性 | 正文写了「最后核实：YYYY-MM-DD」时才与 `last_verified` 比对；只写 `## 来源与更新` 也合法 |
| 渲染 | 旧格式的「- 字段：」仍会被排版成字段列表（兼容存量），新格式按普通 Markdown 渲染，两种都正常显示 |
| 规范 | [条目格式](../meta/条目格式.md) 与 [写作规范](../meta/写作规范.md) 已改写为「文章 + 元数据」模型：小标题由内容决定，唯一固定结构是结尾的 `## 来源与更新` |

## 6. summary 如何生成

规则（`meta.entry_summary`）：

1. 优先用 `meta` 里的 `summary:`；
2. 没有就取正文第一个「自然段」——跳过标题、列表、引用、代码块与分隔线，且长度不少于 20 字；
3. 都取不到才退回标题，并在构建时给出警告。

现状：12 篇已自然化的文章全部带 `summary`；其余 144 篇用正文首段作为摘要，**156 条可读条目无一摘要为空**（有测试 `WritingModel::test_all_complete_entries_have_summary` 兜底）。

## 7. metadata 哪些继续保留

全部保留，且本轮**新增了 `summary`**：

```
id  status  stages  topics  outputs  effort  evidence  last_verified  summary
```

它们继续服务于：搜索（关键词与排序）、筛选（人生阶段 / 方向 / 能换回什么 / 投入 / 证据类型）、导航与阅读顺序、时效检查、构建校验、SEO 静态页。正文不再是表单，但元数据一个都没少。

## 8. 已完成多少篇自然化改写

**12 篇**（每篇都按问题重组了段落，不是把小标题改名）：

| 栏目 | 文章 |
| --- | --- |
| 04 | 读研这笔账，先看目标岗位有没有学历门槛（`grad-school-worth-it`） |
| 04 | 保研和考研，先看资格，再看目标（`baoyan-vs-kaoyan`） |
| 04 | 留学之前，先回答三个能核对的问题（`overseas-is-it-for-me`） |
| 03 | 怎么判断一段实习值不值得（`internship-worth-it`） |
| 05 | Offer 怎么比较：先排除，再拆开（`offer-comparison`） |
| 05 | 简历怎么写：写给筛选者看（`job-hunting-resume`） |
| 05 | 第一份工作最应该换回什么（`first-job-what-to-trade-for`） |
| 06 | 工作头几年应该积累什么（`promotion-first-years`） |
| 07 | 转行为什么不应该清零过去经历（`career-change-keep-capital`） |
| 03 | 本科生怎么开始科研（`research-undergrad-start`） |
| 03 | 什么比赛值得参加（`competition-what-worth-joining`） |
| 02 | 什么证书值得考（`certificate-what-is-worth-it`） |

这些文章的共同变化：开头先讲清问题本身，中间的小标题按判断步骤或分类展开，去掉了「适合谁 / 不太适合谁 / 能换回什么 / 要付出什么 / 怎么开始 / 常见误区 / 下一步可能打开什么」这些栏目名，来源统一收进 `## 来源与更新`。原有的来源逐条照抄，没有删除或改写。

## 9. 哪些文章仍待迁移

**144 篇**（156 - 12），按栏目分布如下：

| 栏目 | 仍是旧模板的条目数 |
| --- | --- |
| 02 在学校里先把基础打好 | 约 10 |
| 03 开始积累真正能留下来的经历 | 约 30 |
| 04 当你开始面对第一次重要分流 | 约 45 |
| 05 从学校走向第一份工作 | 约 10 |
| 06 / 07 / 08 / 11 | 约 40 |

迁移方式已经固化：`tools/naturalize.py` 里每篇文章声明「新标题 + 新正文」，脚本负责替换正文、补 `summary`、保留元数据与来源，并可反复运行（已迁移的会跳过）。下一轮按栏目批量推进即可，不需要再动结构。

## 10. 是否存在内容重复

- **标题层面**：无重复（测试 `NoContentLoss::test_no_duplicate_titles` 全量校验）。
- **条目 id**：无重复（构建校验 + 测试）。
- **结构层面**：专题手册、时间线、工具模板都只做索引与资料，判断一律链接回主线；这条规则写进了 [写作规范](../meta/写作规范.md) 与 [CONTRIBUTING](../CONTRIBUTING.md)，并加了测试（`TopicManual::test_manuals_point_to_main_text`、`test_timelines_are_navigation`）。
- 需要留意的一处：`03` 与 `04` 都涉及科研（初体验 vs 深入），`06` 与 `07` 都涉及跳槽（第一次 vs 分岔）。目前按「同一篇文章只能放在一处、另一处放链接」处理，没有复制正文。

## 11. 是否存在孤儿页面

**没有。** 64 个页面全部可达：导航覆盖 25 个、内容路线图覆盖 39 个（planned）、其余通过正文内链互相引用。检查脚本按「最长前缀匹配」还原导航里的 location 后再判断，避免把带锚点的分组链接误判成孤儿。

## 12. 测试与 CI 状态

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 内容与工程规则测试 | `python3 tools/run_tests.py` | **43 项全部通过**（原 24 项 + 本轮新增 19 项） |
| 统一严格构建 | `python3 tools/build.py --strict --report` | 0 错误 0 警告 |
| 前端冒烟 | `python3 tools/smoke_test.py` | 5 个用例 21 条断言（新增旧深链重定向） |
| CI | GitHub Actions | strict 构建 + 测试 + 产物断言 + 声明审计 |
| Pages | GitHub Actions | 必须 strict 通过才部署 |

本轮新增测试覆盖的正是这次重构的风险点：12 个一级栏目的顺序、书籍文件编号与 section 一致、docs 必须归入 09/10、旧地址重定向可达、条目总数与基线一致（279 条：156 完整 + 110 规划 + 13 首页入口）、摘要不依赖「一句话」栏、12 篇自然化文章的结构、专题页必须引用主线。

---

## 附：一个必须记住的解析约定

旧解析器把**任何 `##` 标题都当成章节分组**，于是文章里的小节标题会把条目提前截断——第一版自然化之后，
`Offer 应该怎么比较` 在页面上只剩第一段，其余小节全部丢失。

修法是把约定说清楚并落到解析器里：

| 写法 | 含义 | 判别 |
| --- | --- | --- |
| `## 分组标题` + 紧跟 `### 条目` | 章节分组（决定左栏与页内目录第二层） | 下一个非空行是 `### ` 或 `## ` |
| `## 小节标题` + 紧跟正文 | 文章小节 | 下一个非空行是段落 / 列表 / 引用 |

这条约定同时写进了 [写作规范](../meta/写作规范.md)，并有回归测试
（`EntryBoundaries`：分组标题切分条目、文章小节留在条目内、分组归属正确）。

## 附：本轮顺带修掉的其它问题

1. **一级栏目排序依赖文件路径**：`book` 与 `docs` 混排时，10 时间线与工具排到了 11 避坑后面。改为按 `section_order` 排序（这是它本来的用途）。
2. **09 专题手册整栏消失**：它的内容当时全是 planned，被隐藏后整栏为空。补了 10 个只做索引的专题手册页，让这一栏有真实入口，同时不复制正文。
3. **贡献者文档混进读者导航**：`meta/` 下的四份规范改为 `nav: false`，页脚里的链接改指 GitHub 原文。
4. **旧章节名的交叉引用**：迁移脚本与后续修正共改写了 79 + 3 处链接，构建的内部链接与锚点校验为零错误。
5. **旧构建产物残留**：改名后遗留的章节页与静态入口页会随构建自动清理（本轮清掉 16 + 3 个）。

## 附：这轮**没有**做的事

- 没有新增正文（除了 10 个专题索引页与约 46 条 `status: todo` 占位，用来把新结构的骨架立起来，它们不进搜索与筛选）。
- 没有引入框架、没有改技术栈、没有动视觉系统、没有动 SEO 静态页机制（仍随构建生成，旧的 hash 深链与静态入口都有效）。
- 没有删除任何既有来源与 `last_verified`。
