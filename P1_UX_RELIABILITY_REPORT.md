# P1 收敛报告：把知识库变成可以公开给陌生人的手册

本轮不做新功能、不扩章节、不重写 UI 框架。目标只有一个：**让已经存在的内容更可信、更好找，
并且不让读者看到施工现场。**

- 仓库：https://github.com/Sver0411/OpportunityHandbook
- 在线：https://sver0411.github.io/OpportunityHandbook/
- 内容规模（本轮未变）：68 份正文、233 条条目（156 条写完 / 64 条规划中 / 13 条首页入口）

---

## 1. planned 内容是否已从用户界面隐藏

是，四处出口全部关闭，仓库里仍保留路线图：

| 出口 | 处理 |
| --- | --- |
| 左侧导航 | 只收录 `complete` / `partial`；planned 一律不进（原先 39 份 planned 专题全部出现在导航里） |
| 搜索 | `index.entries` 只含 `status: complete` 的条目；planned 文档的条目不在其中 |
| 筛选 | 同上（筛选与搜索共用同一份条目集合） |
| 上一页 / 下一页 | 阅读序列只由用户可见文档构成，planned 不参与 |

- 新增 `docs/ROADMAP.md`：集中列出 39 份计划中的专题文档与 64 条规划中的条目；不进导航
  （`nav: false`）。构建会校验「所有 planned 文档都被路线图列出」，漏了就报警告，strict 下失败。
- 旧链接仍可打开（`all_docs` 保留全部文档用于路由解析），页面上会显示
  「本页还没有撰写正文」并指回首页与路线图，不再只是干巴巴一句「尚未撰写」。
- 相关测试：`tools/tests/test_content_rules.py::PlannedContent`（5 项）。

## 2. CI / Pages 是否 strict

是，两者共用同一个入口：

```bash
python3 tools/build.py --strict --report   # CI
python3 tools/build.py --strict            # Pages 部署前
```

`--strict` 会让这些情况失败：元数据错误、重复 id、非法枚举、内部链接与锚点错误、
**元数据解析警告**、构建失败、外链里的永久失效。`403 / 429 / 5xx / 超时 / TLS` 只警告。

同时补上了原先缺失的一环：Pages workflow 之前只跑 `tools/build.py`（非 strict），
存在「CI 不合格但照样部署」的可能；现在它必须在 strict 下构建成功才上传产物。

## 3. stale 是否统一为 365 天

是。原先代码是 `STALE_DAYS = 396`（约 13 个月），而页面文案与 workflow 写的是「12 个月」。

- 代码：`tools/meta.py` → `STALE_DAYS = 365`。
- 文案统一为「超过约 12 个月未重新核实」：站点页内提示、页脚、时效检查 workflow。
- 边界有测试：`StaleContract::test_boundary`（366 天进清单 / 364 天不进）。
- 当前仓库：0 条待重新核实（阈值 2025-09-21）。

## 4. 外链检查如何区分 permanent / transient

`tools/check_links.py` 重写为三分类，并且**只对永久失效返回非零**：

| 分类 | 判定 | CI |
| --- | --- | --- |
| PASS | 2xx，或跟随 301/302/307/308 后仍是 2xx | 通过 |
| WARN | 401/403/405/429/5xx、超时、TLS 握手失败、连接被重置 | 只打印，不阻塞 |
| FAIL | 404 / 410 / 451、域名不存在、URL 格式非法 | 失败 |

输出汇总格式：`External links: PASS 34 · WARN 75 · FAIL 0`。

顺带修了一个**覆盖缺口**：原先只扫描 Markdown 链接语法 `[文字](url)`，而本项目「证据与来源」
里的官方地址大多是纯文本 URL——刚好是最该被检查的那部分。现在两种写法都收（跳过代码块里的示例），
采集量从 59 个升到 **109 个**唯一链接。

两端实测：

| 环境 | 结果 | 说明 |
| --- | --- | --- |
| 本机（macOS + 本机代理） | PASS 34 · WARN 75 · **FAIL 0** | WARN 主要是国内站点的 TLS 握手失败（35 个）与 502/412 反爬（8 个） |
| GitHub Actions | PASS 88 · WARN 20 · **FAIL 1** | 抓到 1 个真失效链接 |

**这套机制上线后的第一次 CI 就抓到一个货真价实的失效来源**：
`book/06-找工作.md` 里《个人信息保护法》引用的 `gov.cn/xinwen/2021-08/...` 旧路径已 404
（gov.cn 改版后旧路径下线）。已改为引用法律名称与条文号，并指向全国人大常委会的
国家法律法规数据库 `flk.npc.gov.cn` 作为检索入口。

换句话说：以前这类失效会永远安静地留在页面上，现在会让 CI 变红。

## 5. Absolute Claim Audit：发现多少、改了多少

新增 `tools/audit_claims.py`：只扫描 `status: complete` 的条目，逐个命中给出
文件 / 条目 id / 行号 / 命中词 / 原句 / 证据类型，并区分「可保留」（有官方规则、研究或行业数据支撑）
与「建议改写」（纯经验判断）。**只报告，不阻塞 CI。**

| | 命中 | 其中建议改写 |
| --- | --- | --- |
| 首次扫描 | 147 | 50 |
| 修改后 | 116 | 33 |

本轮实际改了 **35 处**，包括题目里点名的四条：

1. 「方向不明时，唯一有效的动作是……」
   → 「方向不明时，比继续做抽象判断更有效的一种方式，是完成一次低成本的真实体验，并拿到外部反馈。」
2. 「有资格优先保研，没资格才谈考研」
   → 「先确认自己是否具备推免资格；如果具备，再比较保研能够进入的学校和方向是否符合目标；
   没有推免资格时，再把考研、就业或其他升学路径作为主要选择。」
3. 「标准只有一条：结束时你手上多了什么别人能验证的东西」
   → 「一个非常重要的判断维度是：结束以后你能留下什么别人可以验证的成果。」
4. 进组前置条件（原为「先修完基础课、能独立跑通一个复现」）→ 跨学科版本，
   明确「不需要先达到能独立完成研究的程度」，小任务按学科列举
   （复现代码 / 整理文献 / 协助实验 / 数据清理 / 访谈编码 / 档案整理 / 田野记录 / 设计调研）。

其余改动集中在把经验性表述从「唯一 / 永远 / 一定」改成「通常 / 主要是 / 最直接」，
涉及留学文书与推荐信、专家路线、降薪参照、会议资助来源、社区材料等 12 个条目。

保留未改的 33 处，主要是三类：合规性表述（「必须以当年公告为准」「必须逐校核实」）、
描述**常见误区**的句子（「以为读了研就一定更有选择权」）、以及「优先选…」这类在「怎么开始」
里给出的建议。它们的共同点是**没有被伪装成普遍规律**。

## 6. 首页技术偏置改了什么

- 低成本探索的候选清单从「开源 / 数据竞赛 / 复现论文」扩成跨领域组合：
  开源小贡献、志愿服务并负责真实交付、设计或写作挑战、小型用户调研或商业案例分析、
  跟随从业者或工作坊的体验、维修与手工类小项目、短周期比赛、公开分享、科研小任务。
  并明确写上「不必都和技术有关」。
- 科研入门不再以「复现」为唯一路径，按学科给出小任务示例（见上）。
- 效果：非技术读者进入首页后的第一屏不再只看到代码与数据类例子。
- 相关测试：`ClaimAudit::test_tech_bias_removed_from_exploration`。

## 7. 左侧导航从几层降到几层

**5 层 → 3 层**（领域 → 问题组 / 专题分类 → 文档），导航节点从 5 层树（含全部条目）降为 **94 个节点**。

- 条目不再铺在左栏：条目改由正文与右侧「本页目录」承担；`index.json` 仍保留全部 156 条条目
  供搜索、筛选与深链使用（深链 `#/doc/…/<entry-id>` 未变）。
- 分组链接带锚点：点击「本科 → 硕士」直接落到该分组的标题处（`#/doc/book/03-升学/本科-硕士`）。
- 顺带清掉施工语言：8 个章节里散落的「## 待写条目」分组把 21 条内容并入了对应主题分组，
  左栏不再出现「待写条目」。搬移前后条目 id 完全一致（233 条，无丢失，有脚本核对）。
- 相关测试：`NavigationDepth`（4 项）。

## 8. 搜索 alias 支持哪些词

新增数据文件 `site/search_aliases.json`（60 个键），组内取「或」，不同词之间仍取「与」：

```
保送→保研/推免        读研→硕士/考研/保研      读博→博士
套磁→联系导师/联系教授  换工作→跳槽/转岗        改行→转行
比赛→竞赛              奖学金→scholarship       全奖→全额资助/funding
实习→internship        科研→research            论文→paper/publication
作品→portfolio/项目    副业→side project/自由职业 创业→startup
留学 / 考公 / 考编 / 上岸 / 面试 / 简历 / 证书 / 英语 / 开源 / gap / 间隔年 /
专科 / 专升本 / 非全 / 二战 / 调剂 / 申博 / phd / offer / 谈薪 / 内推 / 背调 /
社保 / 落户 / 志愿 / 公益 / 社团 / 情绪
```

排序同步细化：标题完全一致 > 标题前缀 > 标题包含 > 摘要 > 元数据 / 标签 > 正文命中。

语义示例（有测试保证）：`日本 套磁` 的结果必然是 `套磁` 结果的子集，即两组之间是「与」而非「或」。
测试：`SearchAliases`（5 项，含 `保送`、`套磁`、`换工作`、`读研` 必须命中）。

## 9. 首页 stats 是否移除

是。首页不再显示「已写完条目 / 主题分组 / 规划中条目 / 页面构建日期」这块 Dashboard，
`insertStats()` 已删除。这类项目进度信息移到 [docs/ROADMAP.md](docs/ROADMAP.md) 与
[CONTRIBUTING.md](CONTRIBUTING.md)，首页第一屏现在是正文本身。
测试：冒烟用例「首页不再有项目 Dashboard」。

## 10. book / docs 职责是否明确

明确，写进了规范与页面：

- [meta/写作规范.md](meta/写作规范.md) 新增「book 与 docs 的分工」：正文写判断、方法、路线、决策框架；
  专题写参考资料、时间线、国家、行业、模板与工具；专题不重复正文判断，需要时引用主指南。
- [docs/README.md](docs/README.md) 用一张表说明分工，并写明「还没写的专题列在路线图」。
- 39 个专题文件顶部统一加上「本页是专题资料，判断与方法见主指南 [对应章节]」的指向，
  把科研与学术 ↔ 专题·科研方法、竞赛与挑战 ↔ 专题·竞赛专题这类潜在重复，事先钉在引用关系上。

## 11. 筛选页是否简化

是。默认只展开两维：**人生阶段 / 方向**；「能换回什么 / 投入 / 证据类型」收进「更多筛选」。

- 已选中的高级条件即使折叠，也会在面板顶部以 active chip 显示，点一下即取消。
- 证据类型在界面上改显示中文（官方规则 / 研究支持 / 行业数据 / 实践经验 / 证据不足），
  英文标识（`Official` 等）放 tooltip，内部 schema 与取值完全不变；搜索仍然认英文词。
- 文案从「共 N 条已写完的条目」改为「共 N 条可读条目」，少一点内部视角。

## 12. SEO / permalink 是否完成

完成，且不动现有 SPA：

- 构建时额外产出 `site/pages/…` 静态入口页 **182 个**（156 条条目 + 26 份可读文档），
  每页都带 `title`、`description`、`canonical`、`og:title/description/url/type`，
  正文直接内嵌（搜索引擎看到的是真内容，不是空壳）。
- 同时生成 `sitemap.xml`（196 条 URL）与 `robots.txt`。
- 旧的 hash 深链（`#/doc/...`）与在线阅读体验完全不受影响；两套入口指向同一份内容。
- 首页问题条目**不**单独生成页面，避免与目标条目产生重复内容。
- 构建时会清理上一轮生成、这一轮不再产出的页面（首轮清理了 13 个），避免陈旧页面被一起部署。
- 产物不入库（`.gitignore`），仍由构建生成，Markdown 保持唯一事实来源。

## 13. smoke test 覆盖什么

`tools/smoke_test.py`：起本地 http 服务 + 无头 Chrome 真跑（不引入 Playwright），
四个短用例共 **18 条断言**，最新一次全部 PASS：

```
== 桌面 · 首页与文档 ==   首页能打开[从这里开始] / 目录有内容[94 行] /
                        首页无 Dashboard / planned 不在目录 /
                        文档能加载[13 个条目] / 页内目录有内容
== 桌面 · 深链 ==         深链能定位 / 深链页面有正文
== 桌面 · 搜索与筛选 ==   搜索[12 条] / 筛选[12 条] /
                        叠加条件收窄[5 条] / 证据筛选[9 条] / 同义词「套磁」[45 条]
== 移动端(500px) ==      菜单能打开 / 能关闭 / 点文章后抽屉自动关闭 /
                        能加载文档 / 无横向溢出[500 vs 500]
```

没有 Chrome 的环境会自动跳过（退出码 0），不影响 CI。这条测试本轮就抓到一个真实缺陷：
`watchHeadings` 用 `"IntersectionObserver" in window` 做能力检测，
接口存在但不可用时会让整页显示「内容加载失败」。

## 14. Wiki 是否关闭

已关闭。通过 API 设置 `has_wiki=false` 并回读确认（`has_wiki: False`）。
这样仓库不会出现「Markdown 正文 + Wiki」两套知识源。

## 15. 测试与 CI 状态

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 内容与工程规则测试 | `python3 tools/run_tests.py` | **24 项全部通过** |
| 统一严格构建 | `python3 tools/build.py --strict --report` | 0 错误 0 警告 |
| 外链检查 | `python3 tools/check_links.py --jobs 16` | 109 链接：PASS 34 · WARN 75 · **FAIL 0** |
| 前端冒烟 | `python3 tools/smoke_test.py` | 18 条断言全部 PASS（桌面 + 移动端） |
| CI（内容校验） | GitHub Actions | strict 构建 + 测试 + 产物断言 + 声明审计（报告） |
| Pages 部署 | GitHub Actions | 必须 strict 构建成功才部署 |

CI 第一次以 strict 运行就拦下了一个真失效来源（见第 4 节），修好后重新跑通；
这正好验证了「CI 不合格就不部署」这条链路是通的。

新增测试覆盖的正是本轮的五个决策点：planned 隐藏、导航三层、365 天边界、
解析 fail-closed、同义词搜索，外加声明审计的结构与已修条目。

## 16. 还剩下的 P0 / P1

**P0：无。** 没有发现会误导读者或会静默放行坏内容的问题。

P1（建议下一轮处理，本轮未做）：

1. **外链 WARN 噪音**：本机 75 条 WARN 主要来自国内站点的 TLS/反爬。CI 环境下会少很多，
   但人工排查时仍不易分辨「真封了」还是「只是拦爬虫」。可以在报告里加一列「上次判定为 PASS 的时间」，
   只对「曾经可达、如今永久失效」的链接发警告。
2. **声明审计的 33 处待判断**：都是「优先选…」这类建议句与误区描述，不影响可信度，
   但可以加一条规则：出现在「怎么开始」字段里的「优先」不算违规，让报告更聚焦。
3. **`docs/tools/` 的模板页计数为 0**：它们是工具页而非条目页，导航里显示的数字为空，
   可以考虑给这类页面加一个「模板」标记，而不是留空。
4. **冒烟测试耗时**：四段用例各起一次 Chrome，本机约 3–5 分钟，且与其它 Chrome 实例并行时
   容易因抢资源超时（已加「超时重试一次」兜底）。目前不进 CI（CI 没有 Chrome）。
5. **`docs/tools/` 的模板页在导航里计数为空**：它们是工具页而非条目页，可以考虑加「模板」标记。

---

## 附：本轮顺带修掉的其它问题

- **内部链接检查过严**：指向仓库根文件（`CONTRIBUTING.md`、`LICENSE`）的链接会被判为「目标不存在」，
  现在允许指向任何真实存在的文件（不校验其锚点）。
- **前端健壮性**：`watchHeadings` 原先用 `"IntersectionObserver" in window` 判断，
  接口被置为 `undefined` 时会抛错并让整页显示「内容加载失败」；改为 `typeof === "function"`。
  （这个 bug 正是冒烟测试发现的。）
- **构建产物污染仓库**：`site/pages/`、`sitemap.xml`、`robots.txt` 加入 `.gitignore`。
- **条目搬移的完整性**：搬「待写条目」时用脚本核对前后 id 集合，确保没有内容在搬动中丢失
  （首轮确实丢了 3 条，已找回并经二次核对确认 233 条齐全）。
